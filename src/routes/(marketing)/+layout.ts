// The marketing homepage stays fully prerendered - plain HTML/CSS/JS,
// served statically by adapter-node. Platform routes are NOT prerendered.
export const prerender = true;
export const ssr = true;
